/**
 * Created by dom on 14.02.2015.
 */
import by.bsu.password.PasswordEqual;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class main {
    public static void main(String[] args) throws IOException{
        /*System.out.println("Hello, user!");
        say.sayHello();
        say ob = new say();
        ob.Args(args);
        System.out.println();

        ob.reverseArgs(args);

        numString.outPutString(args);

        //Вводим настоящий пароль buf
        BufferedReader br = new BufferedReader (new InputStreamReader(System.in));
        String buf;
        System.out.println("Введите пароль");
        buf = br.readLine();

        PasswordEqual passw = new PasswordEqual(buf);

        //Вводим пароль, с которым бцдем сравнивать
        BufferedReader br2 = new BufferedReader (new InputStreamReader(System.in));
        String str2;
        System.out.println("Введите пароль повторно");
        str2 = br2.readLine();

        passw.equal(str2);
        */

    }
}

